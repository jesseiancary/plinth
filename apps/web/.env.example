# API URL (accessed from browser, not container)
# In Docker development: Traffic routes through Caddy reverse proxy (with auto-generated HTTPS)
# Caddy routes /api/* requests to the API container
VITE_API_URL=https://localhost
