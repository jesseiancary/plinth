{
  "name": "@plinth/openapi",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "validate": "redocly lint openapi.yaml",
    "generate:types": "openapi-typescript openapi.yaml -o ../types/src/generated.ts && prettier --write ../types/src/generated.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@redocly/cli": "^2.36.0",
    "openapi-typescript": "^7.13.0"
  }
}
