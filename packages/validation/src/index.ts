/**
 * @plinth/validation
 *
 * Shared Zod validation schemas for the Plinth application.
 * Used by both backend (API validation) and frontend (client-side validation).
 */

export * from './auth.js'
